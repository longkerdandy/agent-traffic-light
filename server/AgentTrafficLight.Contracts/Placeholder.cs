namespace AgentTrafficLight.Contracts;

/// <summary>
/// Placeholder type for the shared contracts assembly. Remove once real shared types are added.
/// </summary>
public static class Placeholder
{
    /// <summary>
    /// Gets the placeholder name.
    /// </summary>
    public static string Name { get; } = nameof(AgentTrafficLight);
}
